global{
	main(int c,char** a){
		var b=0
		var d=0
		loop{
			if b++==10{break}else a=2
			if b++==10{break}else {d=2}

			if b++==10 break else d=2
			if b++==10 break else {d=2}
		}
		printf("\n");
	}
}

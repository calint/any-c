entity{
	var id=0
	var f=2.2
	print{
		printf("entity: %d %f\n",id,f)
	}
	_free{
		printf("free entity %d\n",id);
	}
}

global{
	main{
		var i=1
		loop{
			entity e
			e.id=i++
			e.print()
			if i<4 continue else break
			entity e2
			e2.id=i++
			e2.print()
		}
	}
}